def help_command():
    print("""
    Список команд:

    add <название> --amount=число --email=адрес [--due=ГГГГ-ММ-ДД] [--tags=тег1,тег2,…]
        Добавить заказ. Первый аргумент — название (несколько слов — в кавычках).
        Опции только в виде --ключ=значение (знак = обязателен). Теги перечисляют через запятую без пробелов.
        Пример: add "Учебник Python" --amount=1500 --email=user@mail.ru --due=2026-06-01 --tags=книга,обучение

    list [--overdue] [--tag=…] [--limit=N] — список заказов (в разработке)
    remove --id N — удалить заказ по id (в разработке)
    edit --id N [поля…] — редактирование (в разработке)
    tags --id N … — теги (в разработке)
    status --id N … — статус (в разработке)

    help — эта справка
    exit — выход из программы""")